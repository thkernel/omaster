$("ul.errors").html("");
<% if @payment_type.errors.any? %>
  <% @payment_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".payment-types").html("<%= escape_javascript(render 'index') %>");
  $("#payment-type-modal").modal("hide");
 
<% end %>