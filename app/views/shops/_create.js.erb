$("ul.errors").html("");
<% if @shop.errors.any? %>
  <% @shop.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".shops").html("<%= escape_javascript(render 'index') %>");
  $("#shop-modal").modal("hide");
 
<% end %>