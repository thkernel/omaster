$("ul.errors").html("");
<% if @permission.errors.any? %>
  <% @permission.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".permissions-index").html("<%= escape_javascript(render 'index') %>");
  $("#permission-modal").modal("hide");
 
<% end %>