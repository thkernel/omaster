<% @arrival_mails.each do |arrival_mail| %>
    <tr>
    <td><%= register(arrival_mail.register_id).name %></td>
    <td><%= arrival_mail.departure_date.strftime("%d/%m/%Y") %></td>
    <td><%= arrival_mail.receipt_date.strftime("%d/%m/%Y") %></td>
    <td><%= correspondent(arrival_mail.correspondent_id).organization_name %></td>
   
    

    <td>
    <span class=""><%= link_to "Imputations",arrival_mail_imputations_path(arrival_mail.id) %></span>
    <span class=""><%= link_to "Tâches",arrival_mail_tasks_path(arrival_mail.id) %></span>
	    <span class="action-buttons">
			<%= link_to raw('<i class="fa fa-pencil" aria-hidden="true"></i>
'),  edit_arrival_mail_path(arrival_mail), title: "Modifier", remote: false %>


<%= link_to raw('<i class="fa fa-archive" aria-hidden="true"></i>
'), arrival_mail_archive_path(arrival_mail), remote: true, title: "Archiver", class: "archive" %>



<%= link_to raw('<i class="fa fa-trash-o" aria-hidden="true"></i>
'), arrival_mail_delete_path(arrival_mail), remote: true, title: "Supprimer", class: "delete" %>


</span>
</td>

    </tr>
<% end %>