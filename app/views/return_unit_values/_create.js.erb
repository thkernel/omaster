$("ul.errors").html("");
<% if @return_unit_value.errors.any? %>
  <% @return_unit_value.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".return-unit-values").html("<%= escape_javascript(render 'index') %>");
  $("#return-unit-value-modal").modal("hide");
 
<% end %>