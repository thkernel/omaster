$("ul.errors").html("");
<% if @head_pont.errors.any? %>
  <% @head_pont.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".head-ponts").html("<%= escape_javascript(render 'index') %>");
  $("#head-pont-modal").modal("hide");
 
<% end %>