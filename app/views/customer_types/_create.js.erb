$("ul.errors").html("");
<% if @customer_type.errors.any? %>
  <% @customer_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".customer-types").html("<%= escape_javascript(render 'index') %>");
  $("#customer-type-modal").modal("hide");
 
<% end %>