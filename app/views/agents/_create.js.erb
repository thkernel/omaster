$("ul.errors").html("");
<% if @agent.errors.any? %>
  <% @agent.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".agents").html("<%= escape_javascript(render 'index') %>");
  $("#agent-modal").modal("hide");
 
<% end %>