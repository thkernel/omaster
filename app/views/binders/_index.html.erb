<% @binders.each do |binder| %>
    <tr>
    <td><%= folder_name(binder.folder_id) %></td>

    <td><%= binder.name %></td>
    <td><%= binder.description %></td>
   

    <td>
	    <div class="action-buttons">
			<%= link_to raw('<i class="fa fa-pencil" aria-hidden="true"></i>
'),  edit_binder_path(binder), title: "Modifier", remote: true %>




<%= link_to raw('<i class="fa fa-trash-o" aria-hidden="true"></i>
'), binder_delete_path(binder), remote: true, title: "Supprimer", class: "delete" %>
</div>
</td>


    </tr>
<% end %>